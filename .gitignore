.venv/
venv/
__pycache__/
*.pyc
.env
*.db
*.db-wal
*.db-shm
# Runtime trip data (catalog, imports, archives, music, generated video) — all local
data/
*.mp4
*.mp3
.gstack/
# Private working notes — kept local, not published
STATUS.md
# PyInstaller build artifacts (generated by packaging/build.ps1)
build/
dist/
dist.zip

# Maintainer-only release runbook (private repo name + process); keep local, never publish
PUBLISHING.md

# Claude Code project instructions (maintainer-only); keep local, never publish
CLAUDE.md

# Personal itinerary + internal legal checklist; keep local, never publish
docs/trip-plan.txt
docs/pre-public-legal-checklist.md
